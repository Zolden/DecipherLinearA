# CROSS_PROJECT

Сестринский проект: DecipherEtruscan (C:\OtherProjects\DecipherEtruscan) —
перенос нашего метода на этрусский (обоснование: etruscan_feasibility.md).
Находки оттуда, полезные здесь (тирсенская гипотеза, методика, данные),
будут появляться в его CROSS_PROJECT.md; пользователь переносит их сюда.
Находки ЭТОГО проекта, полезные этрусскому (типологический профиль LA,
биморфы, слот-движок), фиксировать здесь и сообщать пользователю.

## 2026-07-08 — принято ИЗ DecipherEtruscan (их CROSS_PROJECT, §0–§1) и проверено

1. **Гипотеза позиционной типологии операторов** («дейксис в начале —
   предикат в конце»; добыта на этрусском: mi 89% нач, lupu 67% фин) —
   **подтверждена на LA** (§BO): KU-RO финальный (p̃сем=0.0080, предикатный
   тип), A-DU (p̃сем=0.0049) и A-TA-I-*301-WA-JA (p̃сем=0.0001) начальные
   (дейктический тип). Обратная связь записана в их CROSS_PROJECT.md.
2. **Westfall–Young min-p** портирован из их tools/etr_operators.py в
   analyze_operator_typology.py — теперь наша стандартная реализация
   семейного контроля.
3. **Урок редких графем** (их ê живёт только в Кортонской табличке) →
   аудит analyze_rare_signs.py (§BP): вердикт §J по *815 понижен
   (одномонументный знак).
4. **Урок заморозки компилятивных корпусов** (двойные чтения одной надписи,
   языковые примеси) взят на вооружение для будущей интеграции SigLA.

Экспортировано ТУДА ранее: методическое ядро (CLAUDE.md спин-оффа),
типологический профиль LA (§AY), биморфы/морфотактика (§BI/§BK),
слот-движок формулы.

## 2026-07-08 (позже) — принят их п.5 (депрессионный тест суффиксов), итог + встречный вопрос

5. **Комплементарность суффиксов** портирована (§BU,
   analyze_suffix_complementarity.py): на 618 типах LA тест упирается в
   стену мощности (даже ∩=0 на парах 29×30 основ → raw p=0.010, ×182 ≈ 1);
   направление совпадает (топ-депрессия -NA против -TE/-RE/-TI/-JA).
   Вывод: метод требует лексикона в разы больше — граница применимости
   зафиксирована в обе стороны.
   **Встречный вопрос туда:** наш «синтаксис записи» (§BR) нашёл операторы
   ВТОРОЙ позиции (TE 54%, SA-RA2 56%, p̃сем=0.001–0.003, после
   дейктического открытия) и «итоговый блок» в двух финальных позициях
   (KU-RO). Есть ли в этрусском операторы второй позиции (ваккернагелевский
   слот — частицы/связки после mi-открытия)? Записано в их CROSS_PROJECT.
